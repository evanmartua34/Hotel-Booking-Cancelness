The main Data that is used for analysis is the 'hotel_booking.csv'
after preprocessing i modify the data into 'df_done.csv' which would be import in the code as df_2 thas is used to build the model.
To avoid overfitting, here I use nested cross validation algorithm. 